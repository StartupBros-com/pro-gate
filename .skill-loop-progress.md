# Skill Loop Progress
# Skill: extreme-software-optimization
# Target: trusted CI runner (`.github/workflows/ci.yml`, `tests/engine.test.sh`, `tests/cdp-salvage.test.mjs`)
# Total Passes: 10
# Started: 2026-08-31T23:43:51Z
# Tracking: issue #114 / PR #115
# Baseline: `tests/artifacts/perf/2026-08-31-ci-runner/` at merge `83fc083`

## Status: IN PROGRESS — Pass 1 of 10

## Non-negotiable safety contract

- Production timeout, watchdog, probe, process-drain, CDP poll, render, and cleanup defaults remain unchanged.
- Every override is narrowly test-only, strictly parsed, bounded, and falls back to the exact production value.
- One measured lever per productive pass and one independent commit per pass.
- Every implemented opportunity must score Impact × Confidence / Effort ≥ 2.0.
- Preserve exit codes, charged/no-refund behavior, reservation/memo/blacklist/cross-bind state, signal ordering, hydration ordering, and no-orphan guarantees.
- Do not parallelize the current CI test sequence while fixtures share mock ports, state homes, locks, or process cleanup assumptions.

## Missions

1. **Existing hard-cap override (score 25):** Add the existing per-invocation `PRO_GATE_TIMEOUT_GRACE` override only to the three ~165s engine ambiguity fixtures; retain the production 120s default and coreutils kill grace.
2. **Pre-retry probe seam (score 12.5):** Introduce a narrowly test-only deadline override at only the pre-retry CDP probe call; default and malformed/zero fallback remain 30s.
3. **Watchdog observation cadence (score 8):** Add a validated test-only cadence for the watchdog control loop; keep thresholds, kill order, and production 10s cadence unchanged.
4. **TERM-drain/force-settle waits (score 3):** Strengthen signal-order evidence, then make only the TERM-drain and force-settle waits configurable in the TERM-ignoring fixture; production 30s/5s/2s and `--kill-after=30` remain unchanged.
5. **CDP main poll seam (score 10):** Add a positive bounded test-only `POLL_MS` override; retain production 20,000ms and do not alter retry backoff or render timing.
6. **Scratch-render sampling seam (score 8):** Add a nonzero bounded test-only 2.5s sample-interval override while retaining the 25s render budget and hydration semantics.
7. **Classified CDP deadlines (score 3):** Reduce explicit scenario deadlines and child watchdog ceilings only by proven semantic class; no global literal replacement, and reject timing-flaky sub-batches.
8. **Event-confirmed engine waits (score 4):** Replace only delayed-organizer, organizer-join, and no-flock fixed sleeps with ready/released barriers plus bounded timeouts.
9. **Re-profile and re-rank (score 6):** Measure after passes 1–8; do not optimize daemon/distribution/browser in this pass. Open a new opportunity only if the shifted hotspot scores ≥2.
10. **Golden + hosted convergence gate (score 5):** Run all eight CI commands, verify every pass's behavior proof, and collect a controlled hosted profile; stop rather than forcing an unjustified code change.

## Completed Passes

(none yet)

## Stop conditions

- Two consecutive `ZERO-CHANGE` passes with named checks and empty diffs → convergence.
- Same lines flip back and forth → thrashing; stop.
- Pass 10 completes → cap reached.
- No other stop condition is valid.
